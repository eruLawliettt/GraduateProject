@page
@model GraduateProject.Pages.Report.IndexModel
@{
}

<div>
    <div class="d-flex ms-3">
        <a class="btn btn-primary" asp-page="Create" asp-route-planId="">
            <i class="bi bi-clipboard2-plus-fill"></i>
            Сформировать ведомость
        </a>
    </div>

    <div class="d-flex">
        <ul class="list-group m-3">
            @foreach (var report in Model.Reports)
            {
                <li class="card text-white bg-secondary mb-3">
                    <div class="card-header">Ведомость группы: @report.Group.Name</div>
                    <div class="card-body">
                        <h5 class="card-title">Заглушка</h5>
                        <p class="card-text">Заглушка</p>
                    </div>

                </li>
            }
        </ul>
    </div>
</div>
